import { createStitches } from '@stitches/react'

export const {
  config,
  styled,
  css,
  globalCss,
  keyframes,
  createTheme,
  getCssText
} = createStitches({
  theme: {
    colors: {
      Rocketseat: '#8257e6'
    }
  }
})
